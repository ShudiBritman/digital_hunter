# digital_hunter

## The project reads the information, filters out noise and errors, compares with the database, calculates the status, and updates the database. ##


### To present the project ###
```docker compose up -d```